*** Settings ***

Documentation  A resource file containing the ParkCalc app specific keywords and variables that create our own domain specific language. Also SeleniumLibrary itself is imported here so that tests only need to import this resource file.

Library        SeleniumLibrary


*** Variables ***

${BROWSER}       firefox
${DELAY}         0
${PARKCALCPAGE}  http://adam.goucher.ca/parkcalc/

*** Keywords ***

Open Park Calc Page
    Open Browser  ${PARKCALCPAGE}  ${BROWSER}
    Set Selenium Speed  ${DELAY}
    Title Should Be  Parking Calculator

Park Calc  [Arguments]  ${parkingLot}  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Title Should Be  Parking Calculator
    Select From List  Lot  ${parkingLot}
    Input Text  EntryTime  ${entryTime}
    Select Radio Button  EntryTimeAMPM  ${entryAmPm}
    Input Text  EntryDate  ${entryDate}
    Input Text  ExitTime  ${exitTime}
    Select Radio Button  ExitTimeAMPM  ${exitAmPm}
    Input Text  ExitDate  ${exitDate}
    Click Button  Submit
    Run Keyword And Continue On Failure  Page Should Contain  ${expectedPrice}

Valet Parking  [Arguments]  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Park Calc   Valet Parking  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}

Short-Term Parking  [Arguments]  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Park Calc   Short-Term Parking  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}

Long-Term Garage Parking  [Arguments]  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Park Calc   Long-Term Garage Parking  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}

Long-Term Surface Parking  [Arguments]  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Park Calc   Long-Term Surface Parking  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}

Economy Parking  [Arguments]  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
    Park Calc   Economy Parking  ${entryDate}  ${entryTime}  ${entryAmPm}  ${exitDate}  ${exitTime}  ${exitAmPm}  ${expectedPrice}
